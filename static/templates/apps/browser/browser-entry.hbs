<li class="item" data-uuid="{{ entry.uuid }}" draggable="true">

    <div class="item-row">

        <div class="item-name rollable" role="button" data-action="openLink">

            {{#if entry.img}}
            <img class="item-image gold-icon" loading="lazy" src="{{ entry.img }}" alt="{{ entry.name }}">
            {{/if}}

            <div class="name name-stacked">
                <span class="title">{{ entry.name }}</span>
                <span class="subtitle">{{ localize entry.subtitle }}</span>
            </div>

        </div>

        {{!-- TODO: Display dynamic columns as appropriate --}}

        <div class="item-detail item-source {{#unless entry.sourceLabel}}empty{{/unless}}">
            {{#if entry.sourceLabel}}
            <span>{{ entry.sourceLabel }}</span>
            {{/if}}
            {{#if entry.source}}
            <span class="subtitle">{{ entry.source }}</span>
            {{/if}}
        </div>

        <div class="item-detail item-controls">

            {{#if displaySelection}}
            <input type="checkbox" name="selected" value="{{ entry.uuid }}" {{ checked selected }}>
            {{/if}}

        </div>

    </div>

</li>
